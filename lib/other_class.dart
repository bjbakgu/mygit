import 'package:flutter/material.dart';

class otherClass extends StatelessWidget {
  const otherClass({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
